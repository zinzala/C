// this program print the string literal with the help of puts() function.
// no need of format specifier
// not to worry about new line character 
#include<stdio.h>
int main(){
    char *ptr = "hello";
    puts(ptr);
    puts(ptr);
    return 0;
}