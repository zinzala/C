// only integral values are allowed
#include<stdio.h>
int main(){
    //enum abc{x = 1, y = 2.5, z = 5}; // 2.5 is float value
    //printf("%d %d %d",x,y,z); // value for y is not an integral constant
    return 0;
}