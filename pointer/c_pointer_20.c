#include<stdio.h>
int main(){
    int arr[] = {1,2,3,4,5,6};
//  printf("%d", *(arr+1)); <-- this is allowed here we are not assigning the value we simply print the content of second index 
    return 0;
}